# README
## 运行
```
python -m src.main
```
## 调整配置文件
`config/`下有所有相关运行配置，均已增加注释。
## 运行结果
主目录下输出txt文件记录所有运行结果；
`fragmentation_reports\`下有碎片率随调度轮次结果，可用于作图。
## 作图
`print.py`默认读取`result`下增加碎片权重和不增加碎片权重文件夹内的最新结果，需要自行复制（后续会修改为直接输出至对应文件夹）。